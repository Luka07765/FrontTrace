import './../globals.css';
import ApolloWrapper from '@/Server/Apollo/Query/Connector/ApolloWrapper';
export const metadata = {
  title: 'Next.js App',
  description: 'Generated by Next.js',
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>
        <ApolloWrapper>{children}</ApolloWrapper>
      </body>
    </html>
  );
}
